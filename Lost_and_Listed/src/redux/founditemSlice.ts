import { createSlice } from "@reduxjs/toolkit";

const founditemSlice = createSlice({
  name: "founditem",
  initialState: {},
  reducers: {},
});

export const {} = founditemSlice.actions;
export default founditemSlice.reducer;
